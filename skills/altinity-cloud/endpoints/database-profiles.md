# DatabaseProfiles

DELETE /profile/{id} — Removes a setting profile from a given cluster
GET /cluster/{cluster}/profiles — Lists all setting profiles for a given cluster
POST /cluster/{cluster}/profiles — Creates a settings profile for a given cluster [name, description]
POST /profile/{id} — Modifies settings profile [name, description]
