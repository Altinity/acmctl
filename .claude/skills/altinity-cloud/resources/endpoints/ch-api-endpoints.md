# CHAPIEndpoints

GET /cluster/{id}/ch-api-endpoints — Displays the list of CH API endpoints for a cluster
POST /cluster/{id}/ch-api-endpoints — Updates the CH API endpoints for a cluster [chAPIEndpoints]
POST /cluster/{id}/ch-api-endpoints/run — Runs a query via CH API endpoint for a cluster [endpoint, method, parameters, user, password]
